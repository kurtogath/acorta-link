"use client";

import React from "react";

const NavBar: React.FC = () => {
    return (
        <nav className="sticky top-0 z-40 w-full flex-none px-2 py-5 backdrop-blur transition-colors duration-500 lg:z-50 lg:border-b lg:border-slate-900/10 dark:border-slate-50/[0.06] ">
            Acorta Link
        </nav>
    );
};

export default NavBar;
